const mongoose=require("mongoose")

const userchema=new mongoose.Schema(
    {
        name:String,
        emai:String,
        password:String
    }
)

module.exports=mongoose.model("userlogin",userchema)